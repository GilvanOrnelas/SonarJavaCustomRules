package com.goff.field.application;

import com.goff.field.application.otherService;
import com.goff.mapaweb.domain.atividade.Atividade;

public class ExpressionOfIfClause {

    private boolean fieldInExpression;
    private boolean fieldInElseExpression;
    private Field expressionFieldCall;
    private Field expressionFieldCallCompare;

    public void ifElseBlock() {
        if (fieldInExpression == true) {

        } else if ("" == fieldInElseExpression) {

        }
        
        if(expressionFieldCall.getValue()){
            
        } else if(expressionFieldCallCompare.value().recursive().equals("oi"))
        
    }
}
